---
ansible-navigator:
  execution-environment:
    container-engine: podman
    image: automationhub.home.com/ee-supported-rhel8
    enabled: false

  playbook-artifact:
    save-as: /home/darrick/playbook-artifacts/{playbook_name}-artifact-{ts_utc}.json

  logging:
    file: /home/darrick/ansible-navigator-log/ansible-navigator.log
    level: debug

  #editor:
  #  command: code -g {filename}:{line_number}
  #  console: false
